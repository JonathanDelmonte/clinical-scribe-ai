/**
 * Substituto de `server-only` durante os testes. Ver `vitest.config.ts`.
 *
 * Vazio de propósito: o pacote original existe só para lançar quando alguém o
 * importa do bundle do cliente, e no Vitest não existe bundle de cliente.
 */
export {};
